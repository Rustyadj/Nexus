import React from 'react';

const Activity: React.FC = () => (
  <div className="page-wrapper">
    <div className="page-header"><h1 className="page-title">Activity</h1></div>
    <div style={{ color: 'var(--text-muted)', textAlign: 'center', marginTop: 60 }}>Activity page — Codex builds this</div>
  </div>
);

export default Activity;
