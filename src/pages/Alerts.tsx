import React from 'react';

const Alerts: React.FC = () => (
  <div className="page-wrapper">
    <div className="page-header"><h1 className="page-title">Alerts</h1></div>
    <div style={{ color: 'var(--text-muted)', textAlign: 'center', marginTop: 60 }}>Alerts page — Codex builds this</div>
  </div>
);

export default Alerts;
