import React from 'react';

const Deploy: React.FC = () => (
  <div className="page-wrapper">
    <div className="page-header"><h1 className="page-title">Deploy</h1></div>
    <div style={{ color: 'var(--text-muted)', textAlign: 'center', marginTop: 60 }}>Deploy page — Codex builds this</div>
  </div>
);

export default Deploy;
