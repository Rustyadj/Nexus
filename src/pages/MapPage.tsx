import React from 'react';

const MapPage: React.FC = () => (
  <div className="page-wrapper">
    <div className="page-header"><h1 className="page-title">Map</h1></div>
    <div style={{ color: 'var(--text-muted)', textAlign: 'center', marginTop: 60 }}>Map page — Codex builds this</div>
  </div>
);

export default MapPage;
