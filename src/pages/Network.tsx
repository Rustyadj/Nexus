import React from 'react';

const Network: React.FC = () => (
  <div className="page-wrapper">
    <div className="page-header"><h1 className="page-title">Network</h1></div>
    <div style={{ color: 'var(--text-muted)', textAlign: 'center', marginTop: 60 }}>Network page — Codex builds this</div>
  </div>
);

export default Network;
